<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Тёмная тема для ночного режима — убирает белую вспышку при старте приложения.
         Compose MaterialTheme берёт управление сразу после запуска Activity,
         но до этого момента система показывает этот window background. -->
    <style name="Theme.From0toLamba" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
