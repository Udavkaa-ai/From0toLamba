# Спецификация мини-игр — 7 архетипов

## Общие правила

- Мини-игра запускается **вместо** текущего потока «изучить» при клике на карточку дела в Входящих
- Каждый архетип хозяина (`personaArchetype`) → своя мини-игра
- Маршрут остаётся тот же: `/charter/:projectId`
- После завершения (победа или поражение) показываются параметры дела: APY (посул) + тип дела
- **Победа** → кнопка «Вложить» активна
- **Поражение** → параметры видны, «Вложить» недоступно; альтернатива — 10 Stars чтобы вложить всё равно
- Интуиция: победа → +3 очка; поражение → 0 очков (штрафа нет)
- Текущая механика Купеческой грамоты (BOYARIN) остаётся, кроме изменения таймера

---

## Архитектура (что нужно сделать)

### Сервер

1. **`CharterService.ts`** — изменить `RANK_TIME_LIMIT` так, чтобы для Боярина (BOYARIN) всегда было 15 сек (все чины = 15)
2. **`CharterService.ts`** — добавить функцию `submitMiniGame(userId, projectId, won: boolean)`:
   - Помечает AmaSession как `charterSubmittedAt = now`, `isIntuitionEvaluated = true`, `intuitionDelta = won ? 3 : 0`
   - Ставит `project.isInbox = false`
   - Инкрементирует `gameState.intuitionScore` на `delta`
   - Вызывает `recomputeRank(userId)`
   - Возвращает `{ delta: number }`
3. **`charterRoutes.ts`** — добавить `POST /api/charter/:projectId/submit-minigame` с `body: { won: boolean }`
4. **`payments.ts`** — добавить `minigame_bypass` в enum фич:
   - `createInvoice('minigame_bypass', projectId)` → создаёт инвойс на 10 Stars
   - `activate({ feature: 'minigame_bypass' })` → `{ success: true }` (без изменений в БД)
   - В bot.ts добавить `createMinigameBypassInvoice`

### Клиент

1. **`api/client.ts`**:
   - `charter.submitMiniGame(projectId, won)` → `POST /charter/:projectId/submit-minigame`
   - `payments.createInvoice` — добавить `'minigame_bypass'` в union тип feature
2. **`CharterPage.tsx`** — добавить ветвление:
   - Если `project.personaArchetype === 'BOYARIN'` → существующий поток (intro → reference → scan → result), таймер 15 сек
   - Иначе → поток мини-игр (загрузка charter → запуск нужного компонента → результат)
3. Новые фазы для не-BOYARIN: `'minigame'` | `'miniresult'`
4. **`MiniGameResultSheet`** — новый компонент результата:
   - Показывает победу/поражение + очки интуиции
   - Показывает параметры дела: APY, тип
   - Победа: кнопка «Вложить» → открывает существующий `InvestSheet`
   - Поражение: кнопка «Миновать» + кнопка «10 ⭐ — вложить всё равно»
5. **6 новых компонентов** в `tg/client/src/components/minigames/`:

---

## Мини-игры по архетипам

### 1. BOYARIN (Царь Горох) — Купеческая грамота ✓ (существующая)

Сетка 4×6 = 24 SVG-печати. Ищи подделки.

**Изменение:** таймер теперь **15 секунд для ВСЕХ чинов** (раньше было 25/20/15/10/5).

Остальное без изменений.

---

### 2. BURATINO — Золотой ключик

**Файл:** `BuratinoGame.tsx`

**Поток:**
1. Показывается ключ-образец (3 секунды)
2. Появляется 7 ключей, 1 из которых совпадает с образцом
3. Игрок тапает правильный ключ
4. Таймер: **15 секунд** (с момента появления 7 ключей)

**Ключи:**
- У каждого ключа: головка (форма) + бороздки (паттерн зубьев)
- Генерируются детерминировано из `gridSeed`
- Различия: количество зубьев, их высота/глубина, форма головки (круг/ромб/квадрат)
- Визуал: SVG ключи

**Победа:** тапнул правильный ключ до истечения таймера
**Поражение:** тапнул неверный ключ или истёк таймер

---

### 3. KOLOBOK — Нора-нора-нора (Whack-a-mole)

**Файл:** `KolobokGame.tsx`

**Поток:**
- Сетка 3×3 норок
- Из норок выскакивают персонажи на **0,5 секунды**
- Заяц, волк, медведь, лиса: тапнуть → **+1 балл**
- Колобок: НЕ тапать, тап → **−3 балла**
- Таймер: **15 секунд**
- Нужно набрать **7 баллов** до конца таймера

**Визуал:**
- Норки — тёмные круглые отверстия
- Персонажи — круги с нарисованными мордочками (emoji-стиль или простая CSS-графика)
- Колобок — жёлтый круг с улыбкой, похож на зверей (нарочно)
- Звери: серый (волк), коричневый (медведь), белый (заяц), рыжий (лиса)

**Победа:** набрал 7 баллов за 15 секунд
**Поражение:** время вышло, набрал меньше 7 баллов (отрицательный счёт тоже = поражение)

---

### 4. KOSCHEI — Память Кощея (Memory match)

**Файл:** `KoscheiGame.tsx`

**Поток:**
- Сетка 3×4 = 12 карточек (6 пар)
- Пары: 🌳 дуб, 🪙 сундук, 🐇 заяц, 🦆 утка, 🥚 яйцо, 🪡 игла
- Игрок открывает по 2 карточки за «попытку»
- Совпали → остаются открытыми
- Не совпали → закрываются
- Лимит: **7 попыток** (= 7 пар открытий)
- Таймер: **15 секунд**

**Победа:** нашёл все 6 пар до исчерпания попыток И времени
**Поражение:** закончились попытки (7 неудачных пар) ИЛИ истёк таймер

**Расположение карточек:** детерминировано из `gridSeed` (перемешивание)

---

### 5. ZOLUSHKA — Золушкино счастье (Coin sorting)

**Файл:** `ZolushkaGame.tsx`

**Поток:**
1. Сначала показывается монета-образец (3 секунды): «настоящий золотой»
2. Монеты начинают падать сверху вниз, вращаясь на 360°
3. Одновременно на экране **5 монет**
4. Настоящие монеты (нужно тапнуть): совпадают с образцом по аверсу
5. Фальшивые (не тапать): другой номинал/другой рисунок на аверсе
6. Тап по настоящей: **+1 к счётчику**
7. Тап по фальшивой: **−1 к счётчику** (или просто игнорируется — уточнить)
8. Таймер: **15 секунд**
9. Нужно поймать **7 настоящих монет**

**Визуал монет:**
- Круги с текстом/иконкой номинала
- Образец: «1 золотой» (с солнышком или ликом)
- Фальшивые: «5 медяков», «1 полушка», «3 гривенника» (другие иконки)
- Монеты падают по случайным X-координатам
- Скорость падения: ~150px/сек

**Победа:** поймал 7 настоящих до конца таймера
**Поражение:** таймер истёк, поймано меньше 7

---

### 6. BABA_YAGA — Котёл (Recipe memorization)

**Файл:** `BabaYagaGame.tsx`

**Поток:**
1. Показывается 5 ингредиентов в правильной последовательности (5 секунд на запоминание)
2. Затем 5 раундов: каждый раз показывается **5 ингредиентов** на выбор
3. Нужно выбрать тот, который был **следующим в рецепте**
4. Всего **15 секунд** на 5 раундов (~3 сек на раунд; таймер общий, а не на раунд)
5. Неверный выбор → немедленное поражение
6. Правильный выбор → следующий раунд

**Ингредиенты (8 штук, 5 выбираются в рецепт):**
- 🐸 лягушка
- 🍄 мухомор
- 🦇 крыло летучей мыши
- 💀 череп
- 🌙 лунный камень
- 🕷️ паучья нить
- 🐺 клык волка
- 🪶 вороново перо

**Генерация рецепта:** из `gridSeed` выбираются 5 ингредиентов в случайном порядке

**Победа:** правильно выбрал все 5 ингредиентов за 15 секунд
**Поражение:** ошибка в выборе ИЛИ истёк таймер

---

### 7. IVAN_DURAK — Переводной дурак (Card matching)

**Файл:** `IvanDurakGame.tsx`

**Поток:**
1. У Ивана и у игрока по **7 одинаковых карт** (идентичные руки)
2. Иван открывает по одной карте
3. У игрока рука показана (7 карт перемешиваются каждый ход)
4. Нужно тапнуть ту же карту из своей руки
5. Верно → карта убирается из обеих рук, следующий ход
6. Ошибка → немедленное поражение
7. Таймер: **15 секунд** на все 7 карт

**Карты:**
- Масти: ♠ ♥ ♦ ♣
- Значения: 6 7 8 9 10 В (валет) Д (дама)
- 7 уникальных карт из колоды, выбираются из `gridSeed`

**Визуал:**
- Карта Ивана: большая, по центру вверху
- Рука игрока: 7 карточек внизу в ряд
- Стиль: белый фон, масть + значение, цвет масти (красный ♥♦, чёрный ♠♣)

**Победа:** скопировал все 7 карт без ошибок за 15 секунд
**Поражение:** ошибка в выборе ИЛИ истёк таймер

---

## Компонент каждой мини-игры (интерфейс)

```tsx
// tg/client/src/components/minigames/<Name>Game.tsx
interface MiniGameProps {
  seed: string                              // charter.gridSeed — для детерминированной генерации
  difficulty: 'EASY' | 'MEDIUM' | 'HARD'   // опционально, для будущего масштабирования
  onComplete: (won: boolean) => void        // вызывается при победе или поражении
}
```

---

## Поток в CharterPage (не-BOYARIN)

```
charter.start(projectId) загрузился
         ↓
project.personaArchetype !== 'BOYARIN'
         ↓
phase = 'minigame'  →  рендерим <XxxGame seed={...} onComplete={handleMiniGameComplete} />
         ↓
handleMiniGameComplete(won: boolean):
  - вызываем api.charter.submitMiniGame(projectId, won)
  - получаем { delta }
  - setPhase('miniresult')
         ↓
phase = 'miniresult'  →  <MiniGameResultSheet won={won} delta={delta} project={project} ... />
         ↓
  Победа → кнопка «Вложить» → <InvestSheet projectId={...} />
  Поражение → кнопка «Миновать» + кнопка «10 ⭐ вложить всё равно»
         ↓
«10 ⭐»: createInvoice('minigame_bypass') → openInvoice → 'paid' → activateMinigameBypass() → <InvestSheet />
```

---

## Примечания

- `gridSeed` из существующего `CharterDTO` используется как источник случайности для всех мини-игр
- Все мини-игры полностью клиентские; сервер получает только `{ won: boolean }` при сабмите
- Для Колобка и Золушки: игра ведётся в реальном времени, сервер не валидирует события — доверяем клиенту
- Минимум зависимостей: только React state + CSS animations (без дополнительных библиотек)
- CSS анимации монет Золушки, выскакивания Колобка — через `@keyframes` в inline style или CSS classes

---

## Статус

- [x] Спецификация согласована
- [ ] Серверные изменения
- [ ] `BuratinoGame.tsx`
- [ ] `KolobokGame.tsx`
- [ ] `KoscheiGame.tsx`
- [ ] `ZolushkaGame.tsx`
- [ ] `BabaYagaGame.tsx`
- [ ] `IvanDurakGame.tsx`
- [ ] Интеграция в `CharterPage.tsx`
- [ ] `MiniGameResultSheet` компонент
- [ ] API: `charter.submitMiniGame`, `payments.minigame_bypass`
