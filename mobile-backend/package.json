{
  "name": "from0tolamba-mobile-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Отдельный мини-бэкенд для Android-версии: AI-прокси к OpenRouter + сбор заметок тестеров. Своя маленькая Postgres, TG-сервера и его БД не касается.",
  "scripts": {
    "start": "prisma generate && prisma db push --accept-data-loss && tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@prisma/client": "^5.22.0",
    "fastify": "^4.28.1",
    "openai": "^4.67.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
